统一会话管理改造技术方案
1. 概述
1.1 定位
统一会话管理是对话业务事实的唯一权威来源。它保存 Session、Turn、Attempt、消息、结构化 Tool Call/Result、已确认输出和 ContextSnapshot，但不调度 Runtime、不管理 Sandbox、不保存文件二进制、S3 地址或高频 Token Delta。
1.2 改造目标
●	引入 Turn/Attempt 模型，支持重试隔离中断输出
●	引入 ContextSnapshot 不可变快照，支持审计回溯和重试一致性
●	引入确认输出幂等提交，按事件 ID 去重
●	引入 message_seq 字段（所有消息单调递增），支撑 ContextSnapshot 水位记录和省略范围追踪
●	同步化架构：Turn 提交同步返回 ID
●	PENDING → RUNNING 隐式触发（commit-output/tool-call 时自动推进）
●	每个 Attempt 各自调 complete 接口标记终态，COMPLETED 时更新 selected_attempt_id
1.3 保留的现有能力
能力	状态
多平台事件接入（/v1/cm/events Kafka Claim Check）	保留，与新 Turn/Attempt 模型并存
ES 搜索	保留，适配新表结构
对话收藏	保留
已读标记	保留
文件系统（Artifact/云盘/审计）	保留
消息表月度分区	保留，扩展到新表


2. 表结构改造
2.1 新增表（context schema，与现有表并存）
2.1.1 turn 表
-- ==================== agi_cm_turn 表 ====================
CREATE TABLE context.agi_cm_turn (
    id                   VARCHAR(64)  NOT NULL,                       -- Turn 唯一标识
    turn_seq_id          BIGINT       NOT NULL,                       -- Turn 递增序号
    session_id           VARCHAR(64)  NOT NULL,                       -- 关联的会话 ID（agi_cm_conversation.id）
    user_message_id      VARCHAR(64)  NOT NULL,                       -- 用户消息 ID（agi_cm_conversation_message.id），一个 Turn 只有一条用户消息
    status               VARCHAR(20)  NOT NULL,                       -- Turn 业务状态：PENDING-等待执行/RUNNING-执行中/COMPLETED-完成/INTERRUPTED-中断可重试/FAILED-失败可重试/CANCELLED-已取消
    active_attempt_id    VARCHAR(64),                                 -- 当前执行中的 Attempt ID
    selected_attempt_id  VARCHAR(64),                                 -- 正式回答的 Attempt ID，指向被选中的成功 Attempt
    source_platform      VARCHAR(64)  NOT NULL,                       -- 来源平台标识（复用 conversation.source_platform）
    completed_time       TIMESTAMP,                                   -- Turn 完成时间
    interruption_reason  VARCHAR(100),                                -- 中断原因，如 RUNTIME_POD_LOST/TIMEOUT/USER_CANCEL 等
    artifact_refs        VARCHAR(64)  NOT NULL,                       -- 发布产物相关信息
    selected_skills      JSONB            ,                           -- 本次用户手动指定的 Skills，包含ID和版本
    create_by            VARCHAR(64)  NOT NULL,                       -- 创建人
    update_by            VARCHAR(64),                                 -- 更新人
    create_time          TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP,  -- 创建时间，分区键
    update_time          TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP,  -- 最近更新时间
    is_deleted           SMALLINT     DEFAULT 0,                      -- 逻辑删除标记：0-未删除/1-已删除
    PRIMARY KEY (id, create_time),
    UNIQUE KEY uk_turn_seq_session (turn_seq_id, session_id)
) PARTITION BY RANGE (create_time);

COMMENT ON TABLE context.agi_cm_turn IS 'Turn 表，一次用户意图（一个问题），只创建一次，可包含多个 Attempt';

-- 月度分区
CREATE TABLE context.agi_cm_turn_202609 PARTITION OF context.agi_cm_turn
    FOR VALUES FROM ('2026-09-01') TO ('2026-10-01');

-- 索引
CREATE INDEX idx_turn_session ON context.agi_cm_turn (session_id, create_time);


2.1.2 attempt 表
-- ==================== agi_cm_attempt 表 ====================
CREATE TABLE context.agi_cm_attempt (
    id                   VARCHAR(64)  NOT NULL,                      -- Attempt 唯一标识
    turn_id               VARCHAR(64)  NOT NULL,                      -- 所属 Turn ID（agi_cm_turn.id）
    attempt_number        INT          NOT NULL,                      -- Turn 内递增的尝试序号，与 turn_id 组合唯一
    trigger_type          VARCHAR(20)  NOT NULL,                      -- 触发方式：INITIAL-首次执行/USER_RETRY-用户重试/INFRA_RETRY-基础设施重试
    status                VARCHAR(20)  NOT NULL,                      -- Attempt 业务状态：PENDING-等待执行/RUNNING-执行中/INTERRUPTED-中断/COMPLETED-完成/FAILED-失败
    context_snapshot_id   VARCHAR(64),                                -- 本次执行使用的上下文快照 ID（agi_cm_context_snapshot.id），重试时复用上次快照
    response_message_id   VARCHAR(64),                                -- 本次 Attempt 产生的 Assistant 回答消息 ID
    interruption_reason   VARCHAR(100),                               -- 中断原因，如 RUNTIME_POD_LOST/TIMEOUT/ARTIFACT_NOT_FOUND 等
    started_time          TIMESTAMP,                                  -- 执行开始时间
    finished_time         TIMESTAMP,                                  -- 执行结束时间
    create_by             VARCHAR(64)  NOT NULL,                      -- 创建人
    update_by             VARCHAR(64),                                -- 更新人
    create_time           TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP,  -- 创建时间，分区键
    update_time           TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP,  -- 最近更新时间
    is_deleted            SMALLINT     DEFAULT 0,                     -- 逻辑删除标记：0-未删除/1-已删除
    PRIMARY KEY (id, create_time)
) PARTITION BY RANGE (create_time);

COMMENT ON TABLE context.agi_cm_attempt IS 'Attempt 表，同一 Turn 的一次执行尝试，可中断可重试，不同 Attempt 输出独立保存';

-- 月度分区
CREATE TABLE context.agi_cm_attempt_202609 PARTITION OF context.agi_cm_attempt
    FOR VALUES FROM ('2026-09-01') TO ('2026-10-01');

-- 索引
CREATE UNIQUE INDEX uk_attempt_turn_num ON context.agi_cm_attempt (turn_id, attempt_number);
CREATE INDEX idx_attempt_turn ON context.agi_cm_attempt (turn_id);


2.1.3 context_snapshot 表（不可变）
-- ==================== agi_cm_context_snapshot 表 ====================
CREATE TABLE context.agi_cm_context_snapshot (
    id                     VARCHAR(64)  NOT NULL,                     -- 不可变快照唯一标识
    session_id             VARCHAR(64)  NOT NULL,                     -- 所属会话 ID
    attempt_id             VARCHAR(64),                               -- 首次使用该快照的 Attempt ID（agi_cm_attempt.id），重试时复用
    through_message_seq    BIGINT       NOT NULL,                     -- 快照覆盖到的消息水位（message_seq 值）
    selected_messages      JSONB        NOT NULL,                     -- 实际选中的消息原文（JSONB），包含保留的消息和 Tool 事件
    summary_blocks         JSONB,                                     -- 对早期历史的结构化摘要（JSONB），超出保留窗口的旧消息被摘要
    omitted_message_ranges JSONB,                                     -- 被省略的消息范围和原因（JSONB），如 tool_result_too_long
    context_policy_revision VARCHAR(20)  NOT NULL,                    -- 裁剪策略版本，改策略时递增
    artifact_refs          VARCHAR(64)  NOT NULL,                     -- 发布产物ID
    content_hash           VARCHAR(64)  NOT NULL,                     -- 整份快照内容的 SHA256 Hash，用于验证重试时快照一致性
    create_by              VARCHAR(64)  NOT NULL,                     -- 创建人
    update_by              VARCHAR(64),                               -- 更新人
    create_time            TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP,  -- 创建时间，分区键
    update_time            TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP,  -- 最近更新时间
    is_deleted             SMALLINT     DEFAULT 0,                    -- 逻辑删除标记：0-未删除/1-已删除
    PRIMARY KEY (id, create_time)
) PARTITION BY RANGE (create_time);

COMMENT ON TABLE context.agi_cm_context_snapshot IS 'ContextSnapshot 表，某次 Attempt 实际看到的上下文的不可变副本，用于审计回溯和重试一致性';

-- 月度分区
CREATE TABLE context.agi_cm_context_snapshot_202609 PARTITION OF context.agi_cm_context_snapshot
    FOR VALUES FROM ('2026-09-01') TO ('2026-10-01');

-- 索引
CREATE INDEX idx_snap_session ON context.agi_cm_context_snapshot (session_id, through_message_seq);
CREATE INDEX idx_snap_attempt ON context.agi_cm_context_snapshot (attempt_id);

2.2 现有表改造
agi_cm_conversation 增加字段
字段	类型	说明
agent_id	VARCHAR(64)	固定 Agent 标识
agent_revision	VARCHAR(100)	固定 Agent 版本
skills_details	JSONB	skills id + skills version
current_message_seq	BIGINT DEFAULT 0	当前消息序号（所有消息递增，用于 ContextSnapshot 水位记录）

不新增 client_app_id（复用 source_platform）、不新增 entrypoint/channel（首版不需要）、不新增 deleted_at（复用 is_delete）。
agi_cm_conversation_message 增加字段
字段	类型	说明
turn_id	VARCHAR(64)	关联 Turn（新链路必填，旧链路为空）
attempt_id	VARCHAR(64)	关联 Attempt（Assistant 回答必填）
message_seq	BIGINT	消息序号（session 内单调递增，所有消息都有值，用于 ContextSnapshot 水位记录和省略范围追踪）

message_seq 与 visible_seq 的区别：message_seq 所有消息都有值（user/assistant/tool），visible_seq 仅 agent 可见消息有值（用于未读计算）。
不迁移旧数据：旧 conversation/message 的 turn_id/attempt_id/message_seq 为空，走旧查询路径；新 Session 走新表结构。
2.3 表关系
┌─────────────────────────┐         ┌──────────────────────────────┐
│   agi_cm_conversation   │ 1    *  │      context_snapshot        │
│      (Session)          │────────│       (不可变快照)             │
└───────────┬─────────────┘         └──────────────────────────────┘
            │ 1                                 ▲
            │                                   │ 0..1 (首次使用)
            │ *                                 │
┌───────────▼─────────────┐         ┌───────────┴──────────────────┐
│         turn            │ 1    *  │          attempt              │
└───────────┬─────────────┘────────│                              │
            │ 1                     └───────────┬──────────────────┘
            │                                   
            │ *                                
┌───────────▼─────────────┐        
│ agi_cm_conversation_msg │         
│      (消息)              │        
└─────────────────────────┘         


关系	基数	说明
Session → Turn	1:N	一个会话有多个用户意图
Session → ContextSnapshot	1:N	一个会话有多个快照
Turn → Attempt	1:N	一个意图可多次执行尝试
Turn → user_message	1:1	一个 Turn 只有一条用户消息
Turn → selected_attempt	1:1	正式回答指向某个 Attempt
Attempt → ContextSnapshot	N:1	多个 Attempt 可复用同一快照
Message → Turn/Attempt	N:1	新链路消息关联 Turn 和 Attempt

2.4 分区策略
表	分区建议	理由
context_snapshot	按 created_time 月度分区	写一次读少，体积大，便于归档
turn	按 created_time 月度分区	与 message 对齐，统一生命周期
attempt	按 created_time 月度分区	同 turn

分区键：统一用 created_at（不可变），行不跨分区移动。跨月执行的 Turn 仍在创建月分区，UPDATE 不改变分区归属。
分区管理：复用现有 CmPartitionScheduleJob（xxl-job），扩展为管理 7 张表的月度分区（agi_cm_conversation、agi_cm_conversation_member、agi_cm_conversation_message、turn、attempt、context_snapshot、tool_call）。预建下月分区 + 清理 N 月前已归档分区。
注意：PG 分区表 PK 必须包含分区键，因此 turn_id 单独不能做 PK，改为 (turn_id, created_at) 联合主键。

3. Turn/Attempt 模型
3.1 模型定义
●	Turn = 一次"用户意图"（一个问题），只创建一次
●	Attempt = 这次意图的"一次执行尝试"，可中断可重试
Turn #1: "帮我写个排序算法"
  ├─ user_message: "帮我写个排序算法"
  ├─ Attempt #1 → INTERRUPTED（半截内容保留但不作为正式回答）
  ├─ Attempt #2 → COMPLETED（用户重试，完整生成）
  └─ selected_attempt_id = #2


3.2 价值
●	同一个问题只记录一次
●	中断的尝试隔离在 INTERRUPTED attempt 里，不污染正式回答
●	context 重建时只用 selected_attempt 的输出
●	"重新生成"时旧回答不进 context（未被 selected）
●	完整审计链：第1次中断了、中断时输出到哪、第2次成功了
3.3 重试规则
相同用户输入的重试仍属于同一 Turn，但创建新的 Attempt。不同 Attempt 的输出独立保存，不能拼接；成功 Attempt 成为当前正式回答。
尚未调用模型、Tool 或修改 Sandbox 的基础设施失败可以在同一 Attempt 内重试；一旦产生确认输出或可能产生副作用，重试必须创建新 Attempt。

4. 同步化架构
4.1 设计决策
前端 → 网关 → agi-context POST /v1/turn/submit
    agi-context DB事务: 写 message + turn(PENDING) + attempt#1
    同步返回 {turn_id, attempt_id, session_id}
网关拿到响应 → 直接开始执行（拿 Lease → 建 ContextSnapshot → 调 cimicode）


4.2 核心价值
●	简单可靠，无异步投递失败风险
●	网关同步拿到 turn_id，自己知道在执行什么
●	不需要 outbox 表、Kafka topic、消费者、定时任务
4.3 交易
●	用同步简单性换掉了异步可靠性
●	后续要加异步解耦再补 Kafka + outbox
4.4 重试和取消也同步化
●	重试：前端 → 网关 → agi-context POST /v1/turn/{turn_id}/retry → 返回新 attempt_id → 网关直接执行
●	取消：前端 → 网关 → agi-context POST /v1/turn/{turn_id}/cancel → 标记 CANCELLED → 网关调 cimicode Cancel

5. ContextSnapshot 存储
5.1 定位
ContextSnapshot 由网关生成，存储在 agi-context（权威存储）。网关 DB 只存 ID + Hash。
5.2 落表时点
时点	操作	触发者
首次执行-生成	INSERT	网关 → agi-context POST /v1/context-snapshot
首次执行-回写 attempt	UPDATE attempt.context_snapshot_id	网关 → agi-context PUT /v1/attempt/{id}/context-snapshot
构建 Invocation	SELECT	网关 → agi-context GET /v1/context-snapshot/{id}
重试-创建 Attempt	INSERT attempt（绑定已有快照 ID）	agi-context 创建 Attempt 时
重试-执行	SELECT（复用）	网关 → agi-context
审计/回溯	SELECT	运维/管理端

核心原则：context_snapshot 表只在首次执行时写一次（INSERT，不可变），之后只读。重试不写新快照，复用已有的。
5.3 重试时快照查找
agi-context 处理 retryTurn 时：
① 查 Turn 状态，确认可重试
② 查上一个 Attempt 的 context_snapshot_id:
   SELECT context_snapshot_id FROM attempt
   WHERE turn_id = ? AND status IN ('INTERRUPTED','FAILED')
   ORDER BY attempt_number DESC LIMIT 1
③ 创建新 Attempt，context_snapshot_id = 上次的快照


5.4 message_seq 与 ContextSnapshot 的关系
ContextSnapshot 中的 through_message_seq 和 omitted_message_ranges（from_seq/to_seq）引用的是 agi_cm_conversation_message.message_seq 字段：
●	through_message_seq：快照覆盖到的最大 message_seq 值
●	omitted_message_ranges.from_seq / to_seq：被省略消息的 message_seq 范围
message_seq 是所有消息（user/assistant/tool）都有的 session 内单调递增序号，由 agi-context 在写入消息时分配。

6. 确认输出幂等提交
6.1 设计
网关消费 cimicode 产出的事件，提交确认输出到 agi-context。每次 commit-output upsert 同一条 message（按 message_id 幂等），累积拼接所有已提交 part 的内容。
POST /v1/attempt/{attempt_id}/commit
{
    "message_id": "msg_xxx",
    "content_type": "text",
    "content": "def sort(arr)...",
    "part_id": "part_xxx",
    "turn_id": "turn_xxx"
}

Response 200: 已提交


6.2 去重机制
事件类型	去重键	去重方式
message.part.updated	message_id	upsert 同一条 message（按 message_id 幂等）
tool_call / tool_result	tool_call_id	UNIQUE(attempt_id, tool_call_id)
done / session.error	turn_id + attempt_id	终态提交幂等（Attempt 已终态则跳过）

6.3 流式 Delta 不落库
assistant_output_delta 不进入长期业务表。确认内容（part.updated）才落 PG。
6.4 PENDING → RUNNING 隐式触发
Turn 提交时状态为 PENDING。当网关首次调 commit-output 或 tool-call 时，agi-context 隐式将 Turn 和 Attempt 从 PENDING 推进到 RUNNING。不需要单独的"开始执行"接口，减少网关调用次数。
如果 cimicode 还没来得及产出任何事件就崩溃了（StartInvocation 后、commit-output/tool-call 之前），Turn 会停在 PENDING。此场景由网关侧 runtime_invocation 心跳超时机制处理，agi-context 不做超时兜底。

7. 接口契约
7.1 接口总览
所有接口通过 Dubbo REST 协议暴露为 HTTP /v1/xxx 路径，前端 → 网关 → agi-context。
7.2 关键接口详细定义
A1: 创建 Session
POST /v1/session/create
Request:
{
    "templatedId":"",
    "skillIds":[{"skillId":"111", "version":"v1"}],
    "agentId":{"agentId":"111", "version":"v1"},
    "source_platform": "cimicode"         // 来源平台标识（复用 conversation.source_platform）
}

Response 200: { "session_id": "ses_xxx" }  // session_id-会话唯一标识
Response 423: { "code": "AGENT_DISABLED", "message": "Agent Revision 已停用" }

Session 创建时不创建 Sandbox、不绑定 Runtime Replica，只固定 Agent 版本和入口信息。标题使用配置默认值（chatProperties.getDefaultConfig().getTitle()），后续由 cimicode runtime 生成标题后回写。同时创建 user 和 agent 两条 member 记录，并同步 conversation 到 ES（含 members nested 数据）。
A2: 提交 Turn
POST /v1/turn/submit
Request:
{
    "session_id": "ses_xxx",              // 会话 ID
    "source_platform": "cimicode",        // 来源平台标识
    "input": "帮我写个排序算法",            // 用户输入文本
    "attachment_ids": ["att_xxx"],        // 附件 ID 列表，可为空
    "selected_skill_ids": [{"skillId":"111", "version":"v1"}]   // 用户显式选择的 Skill ID 列表，可为空
}

Response 200: { "turn_id": "turn_xxx", "attempt_id": "att_xxx" }  // turn_id-Turn唯一标识, attempt_id-首次执行尝试ID

DB 事务：① 校验 Session 存在 ② 分配 message_seq + visible_seq ③ INSERT user_message（turn_id=新turn）④ 更新 conversation 消息计数 ⑤ INSERT turn(PENDING) + attempt#1(PENDING) ⑥ COMMIT → 同步返回。事务提交后异步同步 message + conversation 到 ES。
A3: 重试 Turn
POST /v1/turn/{turn_id}/retry
Request:
{
    "trigger_type": "USER_RETRY"          // 触发方式：USER_RETRY-用户重试/INFRA_RETRY-基础设施重试
}

Response 200: { "turn_id": "turn_xxx", "attempt_id": "att_xxx", "context_snapshot_id": "snap_001" }
    // turn_id-原Turn ID, attempt_id-新创建的 Attempt ID, context_snapshot_id-复用的上下文快照 ID
Response 409: { "code": "TURN_NOT_RETRYABLE", "turn_id": "turn_xxx", "status": "COMPLETED" }
    // Turn 处于终态且不可重试（如 COMPLETED/CANCELLED）


DB 事务：① 查 Turn 状态确认可重试（INTERRUPTED/FAILED）② 查上一个 Attempt 的 context_snapshot_id ③ 创建新 Attempt（context_snapshot_id=上次快照，attempt_number+1）④ 更新 Turn（active_attempt_id、attempt_count、status=PENDING）⑤ COMMIT。网关拿到响应后直接执行，检查已有快照 ID 则复用，不重新生成。
 
A4: 获取 ContextSnapshot 内容
GET /v1/context/snapshot/{attempt_id}
Response 200:
{
    "context_snapshot_id": "snap_001",    // 不可变快照唯一标识
    "session_id": "ses_xxx",              // 所属会话 ID
    "attempt_id": "att_xxx",              // 首次使用该快照的 Attempt ID
    "through_message_seq": 15,            // 快照覆盖到的消息水位
    "selected_messages": [...],           // 实际选中的消息原文（JSONB）
    "summary_blocks": [...],              // 早期历史的结构化摘要（JSONB）
    "omitted_message_ranges": [...],      // 被省略的消息范围和原因（JSONB）
    "attachment_refs": [...],             // 附件稳定引用（JSONB）
    "artifact_refs": [...],               // 产物稳定引用（JSONB）
    "context_policy_revision": "v1",      // 裁剪策略版本
    "content_hash": "sha256:abc123",      // 整份快照内容 Hash，用于验证重试一致性
    "created_at": "2026-09-07T10:00:05Z"
}

Response 404: { "code": "SNAPSHOT_NOT_FOUND", "snapshot_id": "snap_001" }  // 快照不存在


网关构建 RuntimeInvocation 时调用，获取快照内容传给 cimicode。重试时复用同一快照 ID，保证上下文一致。
 
A5: 提交确认输出
POST /v1/attempt/{attempt_id}/commit
Request:
{
    "message_id": "msg_xxx",              // 消息 ID（幂等去重键，相同 message_id upsert 同一条消息）
    "content_type": "text",               // 内容类型：text/markdown/code 等
    "content": "def sort(arr)...",        // 确认的输出内容（累积拼接所有已提交 part）
    "part_id": "part_xxx",                // part ID，标识本次提交的片段
    "turn_id": "turn_xxx"                 // 关联的 Turn ID
}

Response 200: 已提交


每次 commit-output upsert 同一条 message（按 message_id 幂等），内容是累积拼接所有已提交 part。首次调用时隐式将 Turn/Attempt 从 PENDING 推进到 RUNNING，分配 message_seq + visible_seq，更新 conversation 消息计数，事务提交后异步同步到 ES。Assistant 消息的 member_ref 为 conversation.agent_id。
 
A6: 修改 Turn 状态
POST /v1/turn/{turn_id}/update
Request:
{
    "attempt_id": "att_xxx",              // 本次执行的 Attempt ID
    "status": "COMPLETED",                // 终态：COMPLETED-完成/INTERRUPTED-中断/FAILED-失败/CANCELLED-取消
    "reason": "USER_CANCEL"             // 中断原因
}

Response 200: 已提交
Response 409: 已完成（Attempt 已是终态，幂等返回）


每个 Attempt 各自调本接口标记自己的终态。Turn 状态随活动 Attempt 联动：Attempt INTERRUPTED/FAILED → Turn 同步变为 INTERRUPTED/FAILED；Attempt COMPLETED → Turn 变为 COMPLETED + selected_attempt_id = 该 Attempt。Attempt 终态后不可变，重复调用幂等返回。
 
8. 状态机设计
8.1 Turn 状态机（agi-context 侧）
                    ┌──────────┐
                    │ PENDING  │
                    └─────┬────┘
                          │ 隐式触发（首次 commit-output/tool-call）
                          ▼
                    ┌──────────┐
                    │ RUNNING  │
                    └─────┬────┘
                          │
               ┌──────────┼───────────┐
               ▼           ▼           ▼
         ┌──────────┐ ┌──────────┐ ┌──────────┐
         │COMPLETED │ │INTERRUPTED│ │ FAILED   │
         └──────────┘ └──────────┘ └──────────┘
                           │           │
                           │ 用户重试   │ 用户重试
                           ▼           ▼
                     ┌──────────┐ ┌──────────┐
                     │ RUNNING  │ │ RUNNING  │
                     │(新Attempt)│ │(新Attempt)│
                     └──────────┘ └──────────┘

特殊: CANCELLED（用户取消）


PENDING → RUNNING 由 agi-context 在首次收到 commit-output（A10）或 tool-call（A11）时隐式触发，不需要单独的"开始执行"接口。
8.2 Attempt 状态机（agi-context 侧）
                    ┌──────────┐
                    │ PENDING  │
                    └─────┬────┘
                          │ 隐式触发（首次 commit-output/tool-call）
                          ▼
                    ┌──────────┐
                    │ RUNNING  │
                    └─────┬────┘
                          │
               ┌──────────┼───────────┐
               ▼           ▼           ▼
         ┌──────────┐ ┌──────────┐ ┌──────────┐
         │COMPLETED │ │INTERRUPTED│ │ FAILED   │
         └──────────┘ └──────────┘ └──────────┘

Attempt 终态后不可变，重试创建新 Attempt


8.3 终态提交规则
每个 Attempt 各自调 A12（complete）标记自己的终态：
●	Attempt COMPLETED → Turn 变为 COMPLETED + selected_attempt_id = 该 Attempt
●	Attempt INTERRUPTED/FAILED → Turn 同步变为 INTERRUPTED/FAILED
●	Attempt 终态后不可变，重复调用 A12 幂等返回
8.4 状态归属
状态机	归属	存储位置	说明
Turn	agi-context	turn.status	业务状态（权威）
Attempt	agi-context	attempt.status	业务状态（权威）
Invocation	网关	runtime_invocation.status	运行状态（可重建）


9. 故障处理矩阵
#	故障场景	检测方式	处理策略	恢复保证
F1	agi-context DB 不可用	DB 连接异常	提交 Turn 失败返回错误；确认输出提交失败保持幂等重试	Turn 提交原子性（DB 事务）；已提交终态不丢
F2	cimicode 崩溃无事件产出	网关 runtime_invocation 心跳超时	网关检测超时后调 A12 标记 Attempt INTERRUPTED	Turn 不卡死，用户可重试
F3	确认片段乱序	按事件 ID 缓冲或拒绝缺口	网关侧处理	最终一致
F4	最终提交重复	Attempt 终态检查	返回已提交结果	幂等
F5	会话管理不可用	服务健康检查	不启动新 Turn；运行结果持续重试提交	已运行结果不丢
F6	Context 过大	Context Policy 裁剪	网关生成新快照，不让 Runtime 静默处理	显式裁剪记录
F7	ES 不可用	ES 连接异常	降级，不阻塞业务	搜索暂不可用，业务正常

9.1 通用原则
原则	说明
幂等重试	所有写操作幂等
权威存储	agi-context PG 是对话业务事实的唯一权威来源
降级不阻塞	ES/观测系统故障降级，不阻塞 Turn
PENDING 超时由网关处理	agi-context 不做超时兜底，网关侧 runtime_invocation 心跳超时后调 A12


10. 实施分期
阶段 1（P0）：表结构 + 数据访问层 + 接口实现
●	新增 4 张表: turn / attempt / context_snapshot / tool_call
●	现有表改造: agi_cm_conversation 加 agent_id/agent_revision/current_message_seq, agi_cm_conversation_message 加 turn_id/attempt_id/message_seq
●	分区管理扩展: CmPartitionScheduleJob 覆盖 7 张表
●	数据访问层: Entity / Mapper / DBService
●	12 个 Dubbo REST 接口: A1~A12
●	ES 同步: createSession / submitTurn / commitOutput 均同步到 ES
●	Member 注册: createSession 创建 user + agent member
阶段 2（P1）：网关对接
●	网关消费 cimicode 事件 → 调 A10/A11/A12 提交到 agi-context
●	网关 ContextBuilder → 调 A6 拉消息 → 调 A7/A9 存快照
●	网关 SSE 推送 + Resume
阶段 3（P2）：多平台适配
●	现有 /v1/cm/events 保留，适配新表结构（message 带 turn_id）
●	ES 同步适配新表结构
●	新旧链路并存
废弃项（网关上线后）
●	ChatSandboxBizImpl（沙箱管理迁到网关）
●	CimicodeClient（网关 Driver Client 替代）
●	RedisStreamService（网关 Event Ingestor 替代）
●	ChatPromptController（网关 API/BFF 替代）
●	agi_cimicode_session_sandbox_mapping（网关 SessionSandboxBinding 替代）

11. 关键验收标准
1.	重试创建新 Attempt，复用原 ContextSnapshot，隔离中断输出
2.	ContextSnapshot 不可变，content_hash 可验证
3.	确认输出幂等提交，message_id 去重
4.	旧数据不迁移，新旧链路并存
5.	多平台事件接入保留（/v1/cm/events）
6.	月度分区管理覆盖所有新表
7.	message_seq 所有消息都有值，支撑 ContextSnapshot 水位记录
8.	PENDING → RUNNING 隐式触发，无需额外接口
9.	每个 Attempt 各自调 A12 标记终态，COMPLETED 时更新 selected_attempt_id
10.	createSession 同时创建 member + 同步 ES
11.	submitTurn / commitOutput 同步 message + conversation 到 ES

12. 唯一键汇总
唯一键	表	用途
UNIQUE(session_id, message_sequence)	agi_cm_conversation_message	消息顺序（现有保留）
UNIQUE(turn_id, attempt_number)	attempt	Attempt 顺序
UNIQUE(attempt_id, tool_call_id)	tool_call	Tool 关联

 
